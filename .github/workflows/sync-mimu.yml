name: Sync mimu into /mimu/

on:
  push:
    branches: [main]
    paths:
      - '.github/sync-mimu'
      - '.github/workflows/sync-mimu.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: StephenSHorton/mimu
          ref: gh-pages
          path: ${{ runner.temp }}/mimu-src
      - name: Copy published site into /mimu
        run: |
          rm -rf mimu _mimu_src
          mkdir -p mimu
          cp -a "${{ runner.temp }}/mimu-src/." mimu/
          rm -rf mimu/.git
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Publish ミーム from StephenSHorton/mimu gh-pages
